#include <bits/stdc++.h>

using namespace std;

void sol(vector<int> memderIds, vector<pair<int, int>> followers, int follower, int following)
{
    c
}

int main()
{
    // Code Here
    vector<int> memderIds;
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        memderIds.push_back(n);
    }

    return 0;
}